// Placeholder scoring function for age level adjustments.
export function adjustForAge(text, level='middle-grade') {
  // Real implementation would simplify vocabulary, sentence length etc.
  return text;
}
